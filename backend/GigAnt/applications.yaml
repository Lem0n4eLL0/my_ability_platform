spring:
  session:
    store-type: redis  # ← Используем Redis вместо JDBC
  redis:
    host: redis
    port: 6379
management:
  endpoints:
    web:
      exposure:
        include: health,info,beans
  endpoint:
    beans:
      enabled: true